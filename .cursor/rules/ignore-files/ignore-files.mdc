---
alwaysApply: false
description: Leia esta regra apenas quando for necessário entender quais arquivos e pastas devem ser ignorados para busca, navegação ou controle de versão no projeto Godot. Use-a ao configurar .gitignore, ao revisar arquivos temporários, de importação ou de build, ou ao automatizar processos que dependam de exclusão de arquivos não essenciais.
---
# Ignore Files & Folders

- Ignore the `.godot/` directory and all its contents; these are generated by the Godot editor.
- Ignore all `.import` folders and files; these are managed by Godot's import system.
- Ignore all `.tmp` files; these are temporary and not needed for version control or navigation.
- Ignore the `android/` directory if present; it's for platform-specific builds.
- Do not edit or commit these files to version control.
- Keep your `.gitignore` up to date with these patterns.

## Example `.gitignore` additions
```
.godot/
**/.import
**/*.tmp
/android/
```
